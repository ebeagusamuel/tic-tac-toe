name: Testing Github Actions
on:
  push
jobs: 
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: 2.6
      - run: ruby ./.github/workflows/test.rb -e "TestClass.new '---I was called---'.run"